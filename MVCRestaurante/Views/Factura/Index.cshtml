@model MVCRestaurante.ViewModels.FacturaViewModel
<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #000;
            color: #fff;
            margin: 0;
            padding: 0;
        }

        .menu-container {
            max-width: 15000px;
            margin: 3cm auto 0;
            background-color: #2c2f34;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            text-align: center;
        }

        h1 {
            text-align: center;
            color: #fff;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            background-color: #35383d;
        }

        th, td {
            padding: 10px 12px;
            border: 1px solid #ccc;
            text-align: center;
        }

        th {
            background-color: #000;
            color: #fff;
        }

        tr:nth-child(even) {
            background-color: #404247;
        }

        .btn-group {
            display: flex;
            justify-content: center;
            gap: 5px;
        }

        .btn {
            padding: 8px 16px;
            cursor: pointer;
            border-radius: 5px;
            color: #fff;
            text-decoration: none;
            transition: background-color 0.3s;
        }

        .btn-danger {
            background-color: #B22222;
        }

        .btn-primary {
            background-color: #4169E1;
        }

        .btn:hover {
            filter: brightness(1.1);
        }
    </style>
</head>
<body>
    <div class="menu-container">
        <h1>Menú del Restaurante</h1>

        <table>
            <thead>
                <tr>
                    <th scope="col">Id Factura</th>
                    <th scope="col">Id Orden</th>
                    <th scope="col">Cliente</th>
                    <th scope="col">Total</th>
                    <th scope="col">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.facturas)
                {
                    <tr>
                        <td>@item.IdFactura</td>
                        <td>@item.IdOrden</td>
                        <td>@item.Nombre</td>
                        <td>@item.Total</td>
                        <td>
                            <div class="btn-group" role="group" aria-label="Acciones">
                                <a asp-action="Detalles" asp-route-IdFactura="@item.IdFactura" class="btn btn-danger">Detalles<i class="bi bi-trash-fill"></i></a>
                               
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>
</html>
